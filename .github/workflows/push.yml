# Disabled — this workflow is from the upstream OpenCentauri fork
# and requires organization secrets not available in this fork.
# Do not delete this file (it would re-appear on upstream merges).
name: _disabled
on:
  workflow_dispatch:
    inputs:
      _noop:
        description: 'This workflow is disabled'
        required: false
